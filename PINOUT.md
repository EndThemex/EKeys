# EKeys 引脚说明文档

> 适用硬件：ESP32-S3（基于 `platformio.ini` 中 `esp32-s3-devkitc-1` 配置）
> 框架：Arduino + PlatformIO

本文档按功能模块对所有引脚分配进行整理，便于硬件调试与固件开发查阅。

---

## 1. 引脚分配总览表

### 1.1 按引脚号排序

| 引脚 | 信号名       | 方向 | 模块     | 备注                               |
| :--: | :----------- | :--: | :------- | :--------------------------------- |
| IO1  | LCD_BL       | 输出 | LCD 屏幕 | 背光控制                           |
| IO2  | LCD_CS       | 输出 | LCD 屏幕 | SPI 片选                           |
| IO3  | GPIO3        | 输出 | 电源管理 | 5V 供电使能（升压使能）            |
| IO4  | IO4          | 输入 | 电池管理 | 电池电压 ADC 采样                  |
| IO5  | EC11_SW      | 输入 | 旋钮     | EC11 旋钮按键（按下检测）          |
| IO6  | RA           | 输入 | 旋钮     | EC11 A 相（旋转编码）              |
| IO7  | RB           | 输入 | 旋钮     | EC11 B 相（旋转编码）              |
| IO8  | COL3         | 输出 | 按键矩阵 | 列 3                               |
| IO9  | LRCLK        | 输出 | I2S 音频 | 字选择（WS）/ 左右声道时钟（功放） |
| IO10 | BCLK         | 输出 | I2S 音频 | 位时钟（功放 / 麦克风共用）        |
| IO11 | MIC_DIN      | 输入 | I2S 音频 | ICS43434 数据输出 → ESP32 接收     |
| IO12 | MIC_WS       | 输出 | I2S 音频 | ICS43434 字选择                    |
| IO13 | MIC_SCK      | 输出 | I2S 音频 | ICS43434 串行时钟                  |
| IO14 | AMP_SDOUT    | 输出 | I2S 音频 | ESP32 → MAX98357 数据输出          |
| IO15 | DIN (LED)    | 输出 | RGB LED  | WS2812B 数据输入                   |
| IO16 | COL0         | 输出 | 按键矩阵 | 列 0                               |
| IO17 | COL1         | 输出 | 按键矩阵 | 列 1                               |
| IO18 | COL2         | 输出 | 按键矩阵 | 列 2                               |
| IO19 | USB_D-       | 双向 | USB      | USB D-                             |
| IO20 | USB_D+       | 双向 | USB      | USB D+                             |
| IO21 | LED_PWR_CTRL | 输出 | RGB LED  | LED 供电使能                       |
| IO38 | ROW2         | 输入 | 按键矩阵 | 行 2                               |
| IO39 | ROW1         | 输入 | 按键矩阵 | 行 1                               |
| IO41 | LCD_SDA      | 双向 | LCD 屏幕 | SPI 数据（MOSI）                   |
| IO42 | LCD_DC       | 输出 | LCD 屏幕 | 数据/命令选择                      |
| IO45 | INT          | 输入 | 扩展接口 | 中断输入                           |
| IO46 | ROW0         | 输入 | 按键矩阵 | 行 0                               |
| IO47 | SCL_1        | 双向 | 扩展接口 | I2C 时钟（扩展）                   |
| IO48 | SDA_1        | 双向 | 扩展接口 | I2C 数据（扩展）                   |
| TXD0 | TXD0         | 输出 | 调试串口 | UART 发送                          |
| RXD0 | RXD0         | 输入 | 调试串口 | UART 接收                          |

---

## 2. 按功能模块分组

### 2.1 电源管理

| 引脚 | 信号名 | 方向 | 说明                                                                                             |
| :--: | :----- | :--: | :----------------------------------------------------------------------------------------------- |
| IO3  | GPIO3  | 输出 | **5V 升压使能**。拉高开启外挂模块的 5V 供电，仅在 `NORMAL_POWER_MODE` 下开启，其余模式关闭以省电 |
| IO4  | IO4    | 输入 | **电池 ADC**。用于采样电池电压，建议外部加分压电阻                                               |

### 2.2 USB 接口

| 引脚 | 信号名 | 方向 | 说明          |
| :--: | :----- | :--: | :------------ |
| IO19 | USB_D- | 双向 | USB 数据线 D- |
| IO20 | USB_D+ | 双向 | USB 数据线 D+ |

> ESP32-S3 内置 USB 控制器，无需外置 PHY。该接口用于 TinyUSB CDC + HID（键盘、消费控制、串口）。

### 2.3 EC11 旋钮（板载）

| 引脚 | 信号名  | 方向 | 说明                                     |
| :--: | :------ | :--: | :--------------------------------------- |
| IO5  | EC11_SW | 输入 | 旋钮轴心按下检测（低电平有效，内部上拉） |
| IO6  | RA      | 输入 | EC11 A 相（旋转编码）                    |
| IO7  | RB      | 输入 | EC11 B 相（旋转编码）                    |

> 仅用于本地屏幕导航（不进键映射），配合 ESP32 内部 PCNT 实现旋转计数。

### 2.4 RGB LED（WS2812B）

| 引脚 | 信号名       | 方向 | 说明                         |
| :--: | :----------- | :--: | :--------------------------- |
| IO15 | DIN          | 输出 | WS2812B 数据信号（GRB 顺序） |
| IO21 | LED_PWR_CTRL | 输出 | LED 电源使能（MOSFET 控制）  |

> 共 11 颗 WS2812B，与 11 个物理按键一一对应。

> 建议先拉高 `LED_PWR_CTRL` 再驱动 DIN，避免上电瞬间误亮。

### 2.5 按键矩阵（3 行 × 4 列 = 12 位置，共 11 个物理按键）

#### 行（输入）

| 引脚 | 信号名 | 说明     |
| :--: | :----- | :------- |
| IO46 | ROW0   | 按键行 0 |
| IO39 | ROW1   | 按键行 1 |
| IO38 | ROW2   | 按键行 2 |

#### 列（输出）

| 引脚 | 信号名 | 说明     |
| :--: | :----- | :------- |
| IO16 | COL0   | 按键列 0 |
| IO17 | COL1   | 按键列 1 |
| IO18 | COL2   | 按键列 2 |
| IO8  | COL3   | 按键列 3 |

> 物理矩阵共 3 行 × 4 列 = 12 个位置。
> 其中第一行（ROW0）只有 3 个按键（COL3 位置空置），其余两行各 4 个按键，因此 **实际物理按键共 11 个**（应用键 ID 1\~11）。
> 消抖时间 `DEBOUNCE_TIME_MS = 10`。

### 2.6 LCD 屏幕（SPI）

| 引脚 | 信号名  | 方向 | 说明                   |
| :--: | :------ | :--: | :--------------------- |
| IO1  | LCD_BL  | 输出 | 背光 PWM 控制          |
| IO2  | LCD_CS  | 输出 | SPI 片选               |
| IO42 | LCD_DC  | 输出 | 数据/命令选择（DC/RS） |
| IO41 | LCD_SDA | 输出 | SPI 数据（MOSI）       |

> **注意**：LCD 屏幕的 RST 信号直接硬件拉低，**未分配引脚控制**。

### 2.7 I2S 音频

| 引脚 | 信号名    | 方向 | 说明                                    |
| :--: | :-------- | :--: | :-------------------------------------- |
| IO9  | LRCLK     | 输出 | I2S 左右声道时钟（字选择 / WS，功放用） |
| IO10 | BCLK      | 输出 | I2S 位时钟（功放 / 麦克风共用）         |
| IO11 | MIC_DIN   | 输入 | ICS43434 数据输出 → ESP32 接收          |
| IO12 | MIC_WS    | 输出 | ICS43434 字选择                         |
| IO13 | MIC_SCK   | 输出 | ICS43434 串行时钟                       |
| IO14 | AMP_SDOUT | 输出 | ESP32 → MAX98357 数据输出               |

> 外接 MAX98357（数字功放，I2S 输入）和 ICS43434（数字 MEMS 麦克风，I2S 输出）。
> 功放侧：BCLK=IO10、LRCLK=IO9、SDOUT=IO14。
> 麦克风侧：BCLK=IO10、WS=IO12、SCK=IO13、SDOUT(到 ESP32)=IO11。
> 需在固件中以两套独立 I2S 外设或时分复用方式驱动，避免与 LCD SPI 冲突。

### 2.8 扩展接口（I2C + INT）

| 引脚 | 信号名 | 方向 | 说明          |
| :--: | :----- | :--: | :------------ |
| IO47 | SCL_1  | 双向 | 扩展 I2C 时钟 |
| IO48 | SDA_1  | 双向 | 扩展 I2C 数据 |
| IO45 | INT    | 输入 | 外部中断输入  |

> 用于外挂 ModA（旋钮 + 滑动电位器，地址 `0x06`）、ModB（机械旋钮，地址 `0x08`）等模块。
> 注意：与板载 EC11 旋钮并存时，需要确认总线/中断号分配。

### 2.9 调试串口（UART0）

| 引脚 | 信号名 | 方向 | 说明                                 |
| :--: | :----- | :--: | :----------------------------------- |
| TXD0 | TXD0   | 输出 | UART0 发送（仅烧录用，不再承担日志） |
| RXD0 | RXD0   | 输入 | UART0 接收（仅烧录用）               |

> 注意：当前 `platformio.ini` 已启用 USB CDC，日志与桌面 App 协议均走 USB，UART0 仅保留为烧录口。

---

## 3. 引脚使用注意事项

### 3.1 启动 Strapping 引脚

ESP32-S3 的以下引脚在上电时具有 strapping 功能，使用前需确认不会影响启动：

- **GPIO0**：启动模式（通常已内部处理）
- **GPIO3**（用作 5V 使能）：需确认上电时为低电平，避免误触发升压
- **GPIO45**（用作 INT）：默认为 VDD_SPI 电源引脚，复用前请确认硬件设计
- **GPIO46**（用作 ROW0）：默认为 Boot 模式 strapping 引脚，上电时需保持低电平

> 建议参考 ESP32-S3 datasheet 中 strapping pin 章节确认上电电平要求。

### 3.2 RTC IO 与低功耗

IO1、IO2、IO38、IO39、IO45、IO46、IO47、IO48 属于 RTC 域引脚，可用于 Deep Sleep 唤醒。若使用 `DEEPSLEEP_POWER_MODE`，注意保留至少一个唤醒源。

### 3.3 引脚冲突排查

- LCD SPI（IO1/IO2/IO41/IO42）与 I2S（IO9\~IO14）使用不同引脚，无冲突
- RGB LED（IO15/IO21）与按键矩阵（IO8/IO16/IO17/IO18/IO38/IO39/IO46）无冲突
- 调试串口（TXD0/RXD0）建议独立引出测试点

---

## 4. 模块引脚汇总（速查表）

| 模块           | 使用引脚                          |
| :------------- | :-------------------------------- |
| 电源管理       | IO3, IO4                          |
| USB            | IO19, IO20                        |
| EC11 旋钮      | IO5, IO6, IO7                     |
| RGB LED        | IO15, IO21                        |
| 按键矩阵（行） | IO46, IO39, IO38                  |
| 按键矩阵（列） | IO16, IO17, IO18, IO8             |
| LCD 屏幕       | IO1, IO2, IO41, IO42              |
| I2S 音频       | IO9, IO10, IO11, IO12, IO13, IO14 |
| 扩展接口       | IO47, IO48, IO45                  |
| 调试串口       | TXD0, RXD0                        |

---

> 文档基于引脚分配表静态整理。固件实际配置请以代码为准。
